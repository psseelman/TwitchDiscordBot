To start, remove the word "example" from the example.env file and fill out all of the environmental arguments for 
discord and twitch.

Requirements:
Pipenv
Pip
Python 3.6

To Run:
cd into your project folder and enter 'pipenv run python TwitchDiscordBot.py'
